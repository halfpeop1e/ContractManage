export interface Contract {
    id: number
    name: string
    code: string
    customer: string
    beginDate: string
    endDate: string
    status: string
    content?: string
    amount: number
  }
  